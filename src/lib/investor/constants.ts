/** Cookie carrying a signed Investor Preview session. */
export const INVESTOR_SESSION_COOKIE = "oasis_investor";

/** Investor Preview lifetime (seconds). */
export const INVESTOR_SESSION_TTL_SECONDS = 60 * 60 * 48; // 48 hours

/** Generic attribution when /investor is opened with no usable ref. */
export const INVESTOR_REF_DIRECT = "direct";
